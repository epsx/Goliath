/* Minimal stand-in for the CMake-generated miniz_export.h (we link miniz in
   statically, so no dllexport/visibility annotations are needed). */
#ifndef MINIZ_EXPORT_H
#define MINIZ_EXPORT_H
#define MINIZ_EXPORT
#endif
